import { api, client, hasError, initialise, resetConfig, updateInstanceUrl, updateToken } from '@hotwax/oms-api'

export {
  api,
  client,
  hasError,
  initialise,
  resetConfig,
  updateInstanceUrl,
  updateToken
}