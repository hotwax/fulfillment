name: Verify build

on:
  push:
    branches: [ main ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ main ]
      

jobs:
  call-workflow-passing-data:
    uses: hotwax/dxp-components/blob/main/.github/workflows/common.yml@main
